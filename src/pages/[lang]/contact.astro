---
import Contact from '../../components/pages/Contact.astro';

/** English routes only. French uses localized slugs via src/pages/fr/[slug].astro. */
export function getStaticPaths() { return [{ params: { lang: 'en' } }]; }
---
<Contact locale="en" />
