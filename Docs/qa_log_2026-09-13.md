# QA Log — Longhorn CM

**Date:** 2026-09-13  
**Tester:** Dell  
**Status:** In review — P0 regression re-run passed on a green build. Two catalogue-filter
accessibility defects remain open. Client-approval dependencies remain open and are not verified
by this QA pass.

## Scope and evidence

Tested the local development build at `http://localhost:4321` and reviewed the current source
tree. This log deliberately does not change production code.

## P0 review

| P0 | Result | Evidence |
| --- | --- | --- |
| 1. Internal placeholder copy live | **Pass** | `/en/catalogue/`, `/fr/catalogue/`, and `/en/contact/` contain no `Sample data`, `Map placeholder`, or `Docs/README.md` copy. |
| 2. Catalogue developer alert | **Pass** | No `Load More` control or inline alert is present on EN/FR catalogue. |
| 3. French falls back to English | **Pass** | `/fr/catalogue/` has no tested legacy English placeholder leak; public pages are now component-based, not `PrototypePage.astro`. |
| 4. Focus indicator contrast | **Pass** | Keyboard focus exposes a 3px `#2F7A2A` ring on light surfaces. The token’s documented contrast is 5.6:1. |
| 5. Admin errors styled as success | **Pass (source review)** | `#status.status--ok` and `#status.status--err` use distinct colour plus `✓` / `⚠` textual symbols. |
| 6. Missing 404 | **Pass** | Unknown route renders the branded bilingual 404, with EN/FR recovery links. |

## Keyboard and screen-reader review

- **Pass:** First Tab lands on the skip link; header navigation, language links, CTA and catalogue filters expose keyboard focus.
- **Pass:** Tested focus rings are visible on header controls and on the first catalogue filter controls.
- **Superseded:** Catalogue filters now expose labelled radio groups, `aria-checked`, arrow-key selection, and a polite result count. The remaining defects are recorded in the final re-run below.
- **Pass:** The current accessibility tree provides meaningful links, headings, a labelled primary nav, a labelled language group, and labelled carousel controls on the homepage.

## FR/EN parity

- **Pass (spot check):** EN and FR catalogue routes render localized hero, filter labels, catalogue tags and CTA content; no tested prototype-copy leak.
- **Open dependency:** client approval for final localized-slug/content decisions is outside QA authority and remains unverified.

## Build and Lighthouse

- **Resolved:** the i18n type-parity build blocker is fixed; the final re-run completed with a green build.
- **Lighthouse:** not run. Lighthouse is not installed locally and no locally launchable Chrome binary is available to the CLI. Establish a reproducible local Lighthouse runner (or provide the intended CI/preview command) before using scores as release evidence.

## Final regression gate

### Re-run — 2026-09-13

- **Build:** **Pass.** `npm run build` completes successfully (typecheck and Astro/Netlify build).
  The earlier `whoWeAre.imgAlt` i18n type-parity blocker is resolved.
- **P0 routes:** **Pass.** Re-tested `/en/catalogue/`, `/fr/catalogue/`, `/en/contact/`, and an
  unmatched route. No prototype markers, `Load More`, or inline alert handlers were found; the
  French catalogue contains no tested legacy English leak; the unmatched route renders both EN
  and FR branded 404 recovery content.
- **Focus contrast:** **Pass.** Keyboard focus remains a 3px `#2F7A2A` ring on light surfaces.
- **Catalogue keyboard behavior:** **Partial pass / open defect.** Arrow Right on the Level
  `All` radio correctly moves focus to `Primary`, updates `aria-checked`, and updates the polite
  count to `4 titles`. However, ordinary Tab traversal reaches Level controls, then Subject
  `All`, then drops to `<body>` rather than reaching the remaining Subject/Language controls.
  This is a keyboard-navigation regression.
- **Screen-reader state:** **Open defect.** The `No learning materials match your selected
  filters.` empty-state text and `Clear filters` button are present in the accessibility tree
  while all eight title cards are visible. The visual hidden state does not hide it from assistive
  technology, producing contradictory results.

### Remaining gate

Keep QA **in review**. Re-test the two filter defects above after their fix, then run the
approved Lighthouse environment. Do not mark French-slug/client-content approval dependencies as
complete: they require external client confirmation, which this local regression cannot provide.
